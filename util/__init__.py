"""Engine code for the road-recovery project: UE traffic assignment (ue.py), data loading (io.py),
exact objective evaluation (evaluate.py), and the solvers (oracle, pretrain_milp, greedy, metaheuristic)."""
