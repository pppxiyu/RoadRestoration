"""Visualizations of CORE-LOGIC results (e.g. UE link flows, recovery dynamics).

Note: the toy-DATA figures (network, physical quantities, OD demand, road class,
disruption) are produced by data/siouxfalls_toy/make_instance.py and live under that
folder, not here."""
